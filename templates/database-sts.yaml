apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql
spec:
  serviceName: "postgresql"
  replicas: 1
  selector:
    matchLabels:
      app: linshare-database
  template:
    metadata:
      labels:
        app: linshare-database
    spec:
      containers:
        - name: postgresql
          image: {{ .Values.postgresql.image | quote }}
          env:
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgresql
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgresql
                  key: postgres-password
          ports:
            - containerPort: 5432
          resources:
            limits:
              cpu: "700m"
              memory: "2Gi"
            requests:
              cpu: "500m"
              memory: "1Gi"
          volumeMounts:
            - mountPath: /var/lib/postgresql/data/
              name: pg-data
      imagePullSecrets:
        - name: docker-registry-secret
{{- if and (.Values.postgresql.storageSize) (.Values.postgresql.storageClass) }}
  volumeClaimTemplates:
    - metadata:
        annotations:
          volume.beta.kubernetes.io/storage-class: {{ .Values.postgresql.storageClass | quote }}
        name: pg-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: {{ .Values.postgresql.storageSize | quote }}
{{- end }}
